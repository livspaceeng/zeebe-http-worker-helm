apiVersion: v1
kind: Service
metadata:
  name: {{ include "zeebe-http-worker-helm.fullname" . }}
  labels:
    app: {{ include "zeebe-http-worker-helm.name" . }}
    chart: {{ include "zeebe-http-worker-helm.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "zeebe-http-worker-helm.name" . }}
    release: {{ .Release.Name }}
